# pogema-toolbox-agv

Standalone AGV-oriented map generator toolbox used with `pogema-agv`.

The Python import package remains `pogema_toolbox` for compatibility. It contains
random, maze, room, and warehouse generators from the local vendored toolbox.
